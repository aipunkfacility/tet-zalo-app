# 📜 Changelog
<!-- markdownlint-disable MD024 -->

Все значимые изменения в проекте фиксируются здесь.

## [1.2.0] - 2026-01-29

### Добавлено

- Мультиязычность (i18next): поддержка вьетнамского (VI) и английского (EN) языков.
- Переключатель языков на главной странице (перенесен вниз для удобства).
- Поддержка SASS/SCSS.
- **Загрузка фото**: Добавлена возможность выбора фото из галереи устройства (CameraModule).
- **Сохранение**: Функция скачивания сгенерированного изображения при нажатии "SAVE".
- **Система истории**: Локальное хранилище генераций на базе IndexedDB (idb-keyval).
- **Безопасность**: Настроен Content Security Policy (CSP) для защиты от XSS.
- **Deps**: Обновлены зависимости (npm audit fix).

### Изменено

- Улучшена читаемость кнопок управления (золотой текст/иконки).
- **UI Оптимизация**: Удалена кнопка настроек для упрощения интерфейса.
- **Accessibility**: Увеличен размер кнопки переключения языка для удобства нажатия.
- Оптимизирован макет главной страницы.

### Исправлено

- Исправлено зависание на 100% при генерации (стабилизация HistoryContext).
- Исправлена 404 ошибка для `favicon.ico`.
- Сборка проекта в режиме ES модулей.
- Исправлен React Warning: `Cannot update a component while rendering a different component` в модуле генерации.

### Оптимизировано

- **Сжатие истории**: Переход с PNG на JPG (0.8) для локального хранения, что снизило вес базы данных в IndexedDB на 80%.
- **Stable Context**: Применение memoization (useCallback/useMemo) в HistoryProvider для предотвращения лишних ререндеров.

## [1.1.0] - 2026-01-29

### Добавлено

- Интеграция `framer-motion` для анимаций интерфейса.
- Анимации появления на `HomePage`.
- Пульсирующий индикатор загрузки в `GeneratorModule`.
- Секвентальное появление элементов в `ResultModule`.

### Исправлено

- Восстановлен поврежденный `package.json`.
- Исправлена конфигурация `root` в `vite.config.mts` для поддержки `index.html` в корне проекта.

## [1.0.0] - 2026-01-23

### Added

- Инициализация проекта и Agent System Core.
- Инфраструктура тестирования (Vitest).
- Конфигурация линтинга (ESLint).
- Модульный тест для `prompt-builder`.

### Changed

- Полный рефакторинг структуры: вынос контекста, компонентов и страниц.
- Оптимизация `app.tsx`: разделение логики и представления.
- Обновление зависимостей (node_modules).
- Оптимизация ассетов: Конвертация PNG в WebP. Снижение веса фонового изображения `bg.png` с 6.5МБ до 0.35МБ (-94%).
