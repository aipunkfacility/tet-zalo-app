# 🧠 ZMP-TET Profile (Current State)

Этот файл содержит актуальные правила, текущий стек и инструкции для AI-агента. История изменений перенесена в **[CHANGELOG.md](../docs/CHANGELOG.md)**.

---

## 🛠 Актуальный стек

- **Frontend**: React 18, ZMP SDK, ZMP UI
- **Build/Bundle**: Vite, ZMP-CLI
- **Styling**: Tailwind CSS, PostCSS, SASS
- **Icons**: Lucide React
- **Deploy**: Cloud via `zmp deploy`

---

## 📜 Инструкции и Правила (v1.0)

Ты — **Senior Fullstack Developer**. Ты поддерживаешь проект [PROJECT_NAME].

### 🇷🇺 ПРАВИЛО 0: ЯЗЫК (АБСОЛЮТНОЕ)

| Область | Язык |
| --- | --- |
| Общение, планы, объяснения | **РУССКИЙ** |
| Код, переменные, комиты | Английский |

### 📦 ПРАВИЛО 1: ВЕРСИОНИРОВАНИЕ

- **НЕ ПИШИ** историю изменений в этот файл.
- При завершении задачи:
  1. Создай лог в `docs/history/YYYY-MM-DD_[task_name].md`.
  2. Обнови версию в **[CHANGELOG.md](../docs/CHANGELOG.md)**.

### 👨‍⚖️ ПРАВИЛО 3: ПОДТВЕРЖДЕНИЕ ЧЕЛОВЕКОМ

- **Агент никогда не решает сам, что задача "готова".**
- Финальный `[x]` в `task.md` и запись в `CHANGELOG.md` делаются только ПОСЛЕ одобрения пользователем предоставленных доказательств (логов/скринов).

### 🧠 ПРАВИЛО 4: ПАМЯТЬ ПРОЕКТА (AGENT CORE)

- Для синхронизации документации **ОБЯЗАТЕЛЬНО** используй воркфлоу `/update_memory`.
- Не закрывай сессию, пока `node scripts/verify_docs.js` не подтвердит готовность доков.

### 🧪 ПРАВИЛО 5: ТЕСТИРОВАНИЕ

- Любые критические изменения (логика, роутинг, API) **ОБЯЗАТЕЛЬНО** проверяются локально перед деплоем.
- Деплоить можно только версии, проверенные в браузере или через тесты.

### ⚙️ ПРАВИЛО 6: КОНФИГУРАЦИЯ VITE

- Если `index.html` находится в корне проекта, `vite.config.mts` должен иметь `root: "./"`. Если в папке `src`, то `root: "./src"`.

---

## 📂 Ссылки на документацию

- 📄 [Changelog](../docs/CHANGELOG.md)
- 📋 [Backlog](../docs/BACKLOG.md)
- 🚀 [Developer Guide](../docs/DEVELOPER.md)
